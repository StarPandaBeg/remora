export const configDefinitions = {
    // 'video.frameInterval': {
    //     default: 10,
    //     schema: z.number().int().min(1).max(3_600),
    // },
    // 'video.frames.enabled': {
    //     default: true,
    //     schema: z.boolean(),
    // },
}
